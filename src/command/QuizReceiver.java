package command;

import javax.swing.*;

public class QuizReceiver {

    public void startQuiz(int quizId) {
        // Simulate starting a quiz
        JOptionPane.showMessageDialog(null, "Quiz " + quizId + " started.");
    }

    public void deleteQuiz(int quizId) {
        // Simulate deleting a quiz
        JOptionPane.showMessageDialog(null, "Quiz " + quizId + " deleted.");
    }
}
